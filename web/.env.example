# Public, read-only keys (safe to ship — anon role is SELECT-only under RLS).
# Set these as Cloudflare Pages environment variables too (§9.5).
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key
