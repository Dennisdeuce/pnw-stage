httpx==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
feedparser==6.0.11
ics==0.7.2
# ics 0.7.2 leaves tatsu unpinned; tatsu>=5.16 dropped the `buffer_class` setting
# ics passes to ParserConfig, which crashes `import ics` at collection time. Pin to
# the last version ics 0.7.2 imports cleanly against.
tatsu==5.15.1
python-dateutil==2.9.0.post0
tenacity==9.0.0
rapidfuzz==3.10.1
supabase==2.9.1
pytest==8.3.3
